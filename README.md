# 遠近両用コンタクトページ（シティコンタクト コーポレートサイト 追加コンテンツ）

コーポレートサイト <https://www.city-contact.com/> に追加する
**遠近両用レンズのコンテンツページ（熟年世代向け遠近両用カラコン含む）** の制作物一式です。

- ページタイトル：**遠近両用コンタクトレンズ特集｜全店眼科併設で安心の処方**
- 位置づけ：コーポレートサイト内の特集ページ。**WEB広告からの受け皿（ランディング先）**
- クリエイティブコンセプト：**「誰にも気づかれずに、世界が変わる。」**
- 資料：サイト構成案 ページ2 ／ 20260707 定例会企画書に準拠
- 想定URL：`https://www.city-contact.com/enkin/`
- 構成：1ページ完結のコンテンツページ（縦スクロール型）

## ファイル構成

```
.
├── index.html              … ページ本体（このファイルだけで表示可能）
├── assets/
│   ├── css/style.css       … スタイル（配色は :root の変数で一括変更可）
│   ├── js/main.js          … 見え方シミュレーター／フェードイン（ライブラリ不要）
│   └── img/
│       ├── kv-photo.jpg    … メインビジュアルの写真（左＝コンタクトあり／右＝裸眼）
│       ├── kv-main.svg     … （旧）メインビジュアルのイラスト版・現在未使用
│       ├── focus-range.svg … ピントが合う距離の変化
│       ├── mechanism.svg   … 同時視デザインの構造図（遠く／中間／近く）
│       ├── clinic.svg      … 全店眼科併設の処方フロー
│       ├── color-lens.svg  … カラーレンズ4色
│       ├── ogp.svg         … OGP用ビジュアル（1200×630）
│       └── favicon.svg     … ファビコン
├── preview.html            … 確認用の1ファイル版（ダブルクリックで開けます／生成物）
├── tools/
│   └── build-preview.py    … preview.html を作り直すスクリプト
├── docs/
│   ├── site-structure.md   … サイト構成案・原稿の設計意図・掲載時の注意
│   └── creative-reference/ … いただいたクリエイティブサンプル（参考資料）
└── README.md
```

## 表示確認（3つの方法）

### 1. `preview.html` をダブルクリック（いちばん簡単）

CSS・JS・画像をすべて埋め込んだ1ファイル版です。
ダウンロードしてダブルクリックすれば、サーバーもネット接続もなしでそのまま開けます。
※ 内容を更新したら `python3 tools/build-preview.py` で作り直してください。

### 2. GitHub Pages で URL を発行する

1. GitHubのリポジトリを開く → 上部の **Settings**
2. 左メニューの **Pages**
3. Source で **Deploy from a branch** を選択
4. Branch に **`claude/bifocal-contact-page-sgj58u`** ／ フォルダは **`/ (root)`** を選んで **Save**
5. 1〜2分後、`https://rockdanceai2025-sys.github.io/city-contact-enkin/` で閲覧できます

（スマホからも確認でき、URLで共有もできます）

**検索エンジン対策について**
このページには `<meta name="robots" content="noindex, nofollow">` を入れてあるため、
GitHub Pagesで公開してもGoogleなどの検索結果には表示されません。
ただしURLを知っている人は誰でも閲覧できる状態になります（非公開にするにはGitHub Enterpriseが必要）。
社外に出したくない場合は、`preview.html` をメール等で直接お渡しする方法をおすすめします。

### 3. ローカルでサーバーを立てる（開発者向け）

```bash
python3 -m http.server 8000
# → http://localhost:8000/ をブラウザで開く
```

## コーポレートサイトへの組み込み手順

1. `index.html` の `<header>` / `<footer>` / パンくずを、**本番CMSの共通パーツに差し替え**てください
   （本ファイルのヘッダー・フッターは、トンマナ確認用の簡易版です）。
2. `assets/` を `/enkin/assets/` として配置するか、既存のアセットディレクトリへ統合してください。
3. サイト内リンクは `https://www.city-contact.com/...` の絶対パスで記述しています。
   CMS側の運用に合わせてルート相対（`/item/` など）へ変更しても動作します。
4. OGP画像はSVGで用意しています。SNSシェアの互換性を考える場合は
   `assets/img/ogp.svg` を **1200×630のPNGに書き出して** `og:image` を差し替えてください。

## 配色（トンマナ）

`assets/css/style.css` の `:root` にすべて集約しています。
コーポレートサイトの正式なブランドカラーが確定している場合、この数値の差し替えだけで全体に反映されます。

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--c-primary` | `#e4638a` | メインピンク（CTA・アイコン） |
| `--c-primary-dark` | `#c2456c` | 見出し・ホバー |
| `--c-secondary` | `#f292af` | サブピンク（枠線・タグ） |
| `--c-primary-light` | `#fdeff3` | 淡いピンク背景 |
| `--c-cream` | `#faf5ec` | ベージュ背景（フッター等） |

フォントは `Noto Sans JP`（Google Fonts）を使用しています。
コーポレートサイト側で別のWebフォントを読み込んでいる場合は、`--font` を合わせてください。

## 掲載前チェック（重要）

### ⚠️ 本番公開時に必ず削除するもの

`index.html` の冒頭に、確認用サイトを検索結果に出さないための指定が入っています。

```html
<meta name="robots" content="noindex, nofollow">
```

**本番（www.city-contact.com）へ設置する際は、この1行を必ず削除してください。**
残したまま公開すると、本番ページが検索結果に表示されなくなります。
（ルート直下の `robots.txt` も確認用のものなので、本番では不要です）

### 原稿まわり

コンタクトレンズは高度管理医療機器のため、原稿には以下を必ず残してください。

- 「必ず眼科医の検査・処方を受けてお求めください」の注意文（CVセクション内に記載済み）
- 効果効能の断定表現を避ける（本ページは「個人差があります」を各所に明記済み）
- **「全店 眼科併設」**の表現でページ全体を統一しています（2026/08 ご指示にもとづく）。診療日・診療時間のみ「店舗により異なります」と注記
- **製品名**（アイコフレ マルチステージ／スマートフォーカスリング ブラウン）は企画書の記載にもとづく一例です。取扱状況をご確認ください
- **メインビジュアルの写真**は提案書内の画像を切り出したもので、解像度が256×326pxしかありません。**元データのご支給をお願いします**
- **YouTube動画**は準備中のプレースホルダです。公開後、`index.html` 内のコメント部分のiframeに差し替えてください
- 電話番号・受付時間・展開エリアは公開情報をもとに記載しています。最新情報にご確認のうえ差し替えてください

## デザイン案6（`design6/`）— Graphite ink on warm paper

`docs/design/DESIGN_6.md` のスタイルリファレンスに沿った**別トンマナのモック**です。
現行のピンク版（ルートの `index.html`）はそのまま残していますので、並べて比較できます。

| | 現行案 | デザイン案6 |
| --- | --- | --- |
| ファイル | `index.html` ／ `preview.html` | `design6/index.html` ／ `design6/preview.html` |
| 確認URL（GitHub Pages） | `.../city-contact-enkin/` | `.../city-contact-enkin/design6/` |
| 配色 | 白＋ピンク | **完全無彩色**（#0d0d0d／#5d5d5d／#8f8f8f／#f9f9f9／#fff） |
| フォント | Noto Sans JP（Google Fonts） | **OS標準フォント**（Webフォント読込なし＝表示が速い） |
| 文字サイズ | 12〜38px | **14／16／24px の3段階のみ**（強弱は太さと余白で表現） |
| 立体表現 | 影（box-shadow） | **1pxのヘアライン罫のみ**（影は不使用） |
| 角丸 | 16〜26px | 10px（ボタンのみピル） |
| レイアウト | 1カラム＋上部ヘッダー | **左レール（272px）＋読み物カラム（768px）**。左レールは常時追従し、現在地をハイライト |

### 内容は現行案と同じです

原稿・セクション構成・CV導線は現行案と1:1で対応しています（差分はデザインのみ）。
シミュレーター、セルフチェック、4ステップの流れ、医療機器の注意文もそのまま入っています。

### ご確認いただきたい点

- **色を一切使わない**方針のため、図版（`focus-range` ／ `mechanism` ／ `clinic` ／ `ogp` ／ `favicon`）は
  明るさを保ったままグレー化しています（`python3 tools/make-mono-assets.py` で再生成できます）。
  KVの写真もCSSで白黒表示にしています。
- ただし**カラーレンズの図版（`color-lens.svg`）だけは色を残しています**。
  レンズの色は「装飾」ではなく「選ぶための情報」のためです。ここもグレーにするかはご判断ください。
- リファレンスに「24pxより大きい文字を使わない」とあるため、**見出しの最大が24px**です。
  広告の受け皿としてはキャッチが弱く感じられる可能性があります。大きくする場合はご指示ください。
