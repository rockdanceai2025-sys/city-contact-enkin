# 遺品整理・実家じまい ランディングページ（福岡県全域）

いただいた企画提案書「遺品整理サービス ホームページ（LP）要件定義・構成案」にもとづく制作物一式です。

- コンセプト：**心に寄り添う丁寧な遺品整理**
- メインコピー：**「想い出を、ただのゴミにはいたしません。」**
- 対象エリア：**福岡県全域**（構成案の「久留米市・福岡西エリア・佐賀県全域」から、ご指示により訂正）
- 構成：1ページ完結の縦スクロール型LP（構成案の SECTION 01〜08 に準拠）

## ファイル構成

```
ihin-lp/
├── index.html              … ページ本体
├── assets/
│   ├── css/style.css       … スタイル（配色は :root の変数で一括変更可）
│   ├── js/main.js          … フェードイン／フォーム送信のハンドリング（ライブラリ不要）
│   └── img/
│       ├── hero-photo.jpg  … ファーストビューのイラスト（ご支給素材）
│       ├── case1-ba.svg    … 事例①のビフォーアフター図
│       ├── case2-ba.svg    … 事例②のビフォーアフター図
│       ├── ogp.svg         … OGP用ビジュアル（1200×630）
│       └── favicon.svg     … ファビコン
├── preview.html            … 確認用の1ファイル版（ダブルクリックで開けます／生成物）
├── tools/build-preview.py  … preview.html を作り直すスクリプト
└── README.md
```

## 表示確認

### 1. `preview.html` をダブルクリック（いちばん簡単）

CSS・JS・画像をすべて埋め込んだ1ファイル版です。ダウンロードしてダブルクリックすれば、
サーバーなしでそのまま開けます。
※ 内容を更新したら `python3 tools/build-preview.py` で作り直してください。

### 2. パスワード付きの確認用ページとして公開する（クライアント向け）

公開先：`rockdanceai2025-sys/tsumugi-lp`（別リポジトリ／public）

- URL：<https://rockdanceai2025-sys.github.io/tsumugi-lp/>
- パスワード：`tsumugi2026`

公開リポジトリには**暗号化した `site.bin` だけ**を置き、平文は置きません。
URLを開くとパスワード入力画面が出て、正しいパスワードを入れたときだけ
ブラウザの中で復号されてLPが表示されます。

更新するときは、このフォルダで次を実行し、できた `site.bin` を
`tsumugi-lp` リポジトリに置き換えてプッシュします。

```bash
python3 tools/build-preview.py                 # preview.html を作り直す
node tools/build-encrypted.js tsumugi2026      # site.bin を作り直す
```

パスワード入力画面の雛形は `preview-gate/index.html` です
（配色・屋号を変えたいときはこちらを編集）。

### 3. ローカルでサーバーを立てる

```bash
python3 -m http.server 8000
# → http://localhost:8000/ihin-lp/ をブラウザで開く
```

---

## ⚠️ 公開前に必ず差し替えるもの（ダミーデータ一覧）

屋号は **片づけ処 つむぎ**（ご提供いただいた既存サイトより）で確定しています。
下記の項目はまだ仮の値ですので、本番公開前に実際の情報へ差し替えてください。

| 項目 | 現在の値（ダミー） | 出現箇所 |
| --- | --- | --- |
| 電話番号（表示） | 0120-000-000 | ヘッダー／FV／CVエリア／フッター／`main.js` の案内文 |
| 電話番号（リンク） | `tel:0120000000` | 計6か所（`href="tel:..."`） |
| 所在地の**郵便番号・丁目番地** | 未記載（町名まで＝「福岡県久留米市国分町」は確定） | フッター 会社概要 |
| 古物商許可番号 | 福岡県公安委員会 第000000000000号 | フッター 会社概要 |
| 受付時間 | 8:00〜20:00（土日祝も対応・年中無休） | ヘッダー／FV／CVエリア／フッター／追従バー |

一括置換の例：

```bash
cd ihin-lp
sed -i 's/0120-000-000/0120-123-456/g; s/tel:0120000000/tel:0120123456/g' index.html assets/js/main.js
python3 tools/build-preview.py   # プレビューも作り直す
```

### フォームの送信先が未設定です

`index.html` のフォームは `action="#"` のままです。
ご利用のフォームシステム（CMS・メールフォームサービス等）に合わせて `action` / `method` を設定してください。

```html
<form class="p-form" id="js-form" action="#" method="post" novalidate>
```

送信先が未設定のあいだは、送信ボタンを押すと「まだ送信先が設定されていません」という案内を表示します。
`action` を設定すると、この案内は自動的に出なくなり、通常のフォーム送信に切り替わります（`assets/js/main.js`）。

### noindex を削除してください

確認用サイトを検索結果に出さないため、`index.html` の冒頭に次の1行が入っています。

```html
<meta name="robots" content="noindex, nofollow">
```

**本番サーバーへ設置する際は、この1行を必ず削除してください。**
残したまま公開すると、本番ページが検索結果に表示されなくなります。

### OGP画像・canonical

- `og:image` はSVG（`assets/img/ogp.svg`）です。SNSシェアの互換性を考える場合は **1200×630のPNGに書き出して** 差し替えてください。
- `og:image` / `og:url` / `<link rel="canonical">` は相対パスまたは未設定です。公開URLが決まったら絶対URLを設定してください。

---

## 原稿に関する注意（重要）

料金表とサービス内容は**既存サイトの掲載内容**に、事例・お客様の声は**企画提案書の記載**にもとづいています。
実際の実績と異なる場合は、掲載前に必ず差し替えてください。

- **料金表**（1R・1K 25,000円〜／4LDK以上・一戸建て 200,000円〜）… **既存サイト（片づけ処 つむぎ）の掲載内容に準拠**。企画提案書の数値とは異なります
- **事例①** 福岡県久留米市・戸建て4LDK／作業員5名・1.5日／320,000円 − 買取相殺35,000円 = 285,000円
- **事例②** 提案書では「佐賀県鳥栖市」でしたが、**福岡県全域への訂正にともない「福岡県北九州市」に変更**しています
- **お客様の声** … 提案書の記載どおり。1件目の出身地を「佐賀出身」から「福岡県ご出身」に変更しています
- 「追加料金一切なし」「最短即日」などは**実際の運用と一致しているか**をご確認ください（景品表示法上、実現できない表示は不当表示になります）
- 遺品の買取を行う場合、**古物商許可の表示が必要**です。フッターに記載欄を設けていますので、実際の許可番号を入れてください

## 配色（トンマナ）

構成案のご指定「落ち着いた紺・温かみのあるアースカラー」に沿っています。
`assets/css/style.css` の `:root` にすべて集約しているため、数値の差し替えだけで全体に反映されます。

| 変数 | 値 | 用途 |
| --- | --- | --- |
| `--c-navy` | `#23415f` | メインの紺（見出し・フォーム・CVエリア背景） |
| `--c-navy-dark` | `#17304a` | 濃い紺（グラデーション・ホバー） |
| `--c-navy-pale` | `#eef3f8` | 淡い紺背景（お悩みセクション） |
| `--c-earth` | `#b87a44` | アースカラー（CTAボタン・アイコン） |
| `--c-earth-dark` | `#9a6234` | 濃いアースカラー（金額・強調） |
| `--c-earth-pale` | `#fbf1e4` | 淡いベージュ背景 |
| `--c-cream` | `#fbf8f3` | クリーム背景（セクション交互） |

## セクション構成

1. ファーストビュー
2. 共感・お悩み提起
3. **サービス内容（3本柱）** … 既存サイトの「安心の見積もり・追加料金不要／専門的な技術力／丁寧な対応と信頼」を反映
4. 当社が選ばれる5つの理由
5. 遺品整理の料金目安（間取り別） … 既存サイトの料金表を反映
6. 作業実績・ビフォーアフター
7. お客様の声
8. ご依頼の流れ（5ステップ）
9. コンバージョンエリア（電話＋フォーム）

フォントは見出しに `Noto Serif JP`、本文に `Noto Sans JP`（Google Fonts）を使用しています。
どちらも読み込めない環境ではヒラギノ・游ゴシック等にフォールバックします。

## フォントのライセンス

**配信しているフォントは Noto Sans JP / Noto Serif JP のみ**です（Google Fonts 経由）。
どちらも SIL Open Font License 1.1 で、商用利用・改変・再配布が無償で認められています。

CSS・SVG のいずれにも、**Monotype 社のフォント（Helvetica・Arial・Times New Roman 等）は
一切指定していません。** ブラウザの標準指定（`html` は Times New Roman、フォーム部品は Arial）も
明示的に上書きしてあります。

| 対象 | 指定しているフォント |
| --- | --- |
| 本文 | `"Noto Sans JP", sans-serif` |
| 見出し | `"Noto Serif JP", serif` |
| SVG内のテキスト（OGP・事例図） | 同上 |
| ロゴ | フォントを使わない図形のみ（文字のアウトライン化なし） |

フォールバックは `sans-serif` / `serif` の総称のみにしてあります。
Google Fonts が読めない環境では OS 標準のフォントで表示されますが、
これは閲覧者の端末にあるフォントが使われるだけで、配信も複製もしていません。

### 確認のしかた（デベロッパーツール）

1. ページを開いて F12 → **Elements**
2. 要素を選び、右側の **Computed** タブを一番下までスクロール
3. **Rendered Fonts** に、実際に描画に使われたフォント名が出る

`font-family` の指定値は Computed の `font-family` で確認できます。

### 今後フォントを足すとき

- **Monotype 系**：Helvetica、Arial、Times New Roman、Gill Sans、Futura ほか。
  日本語では **フォントワークス（筑紫書体・ロダン・マティス等）が2021年に Monotype 傘下**に入っています。
- 迷ったら、Google Fonts（SIL OFL）か、Adobe Fonts（Creative Cloud 契約の範囲内）に限定するのが安全です。

## シニア視認性への対応（構成案のご指定）

- 本文の基準フォントサイズを **17px**（スマホ16.5px）に設定
- 行間 1.95、文字色 `#3a3630`（白背景に対しコントラスト比 約10:1）
- CTAボタンのタップ領域を大きく確保し、**スマホでは下部に追従バー**（電話発信／Web見積もり）を常時表示
- `prefers-reduced-motion` に対応（アニメーションを無効化）
